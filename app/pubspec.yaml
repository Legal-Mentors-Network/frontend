name: lmn
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  go_router: ^14.2.0
  riverpod: ^2.5.1
  pocketbase: ^0.18.1
  sqflite: ^2.3.3+1
  path: ^1.9.0
  geolocator: ^12.0.0
  geocoding: ^3.0.0
  image_picker: ^1.1.2
  cached_network_image: ^3.3.1
  logger: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
